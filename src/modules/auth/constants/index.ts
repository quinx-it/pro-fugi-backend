export enum AuthEndPoint {
  PREFIX = 'auth',
  ACCESS_TOKENS = 'tokens/access',
  REFRESH_TOKENS = 'tokens/refresh',
  CONFIRMATION_CODES = 'confirmation/codes',
  METHODS = 'methods',
  CURRENT_USER = 'users/user',
  ROLES_CUSTOMER = 'users/user/roles/customer',
}
